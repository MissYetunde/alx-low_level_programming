#include <stdio.h>
/**
 * main - prints its name folloewd by a new line
 * @argv: prints argument vector
 * @argc: prints argument counts
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
